/* import { UPDATE_EDITOR_STATE } from "../actions/types";

const defaultState = {
    editorState: EditorState.createEmpty(),
};

export default (state = defaultState, { editorState, type }) => {
    if (type === 'UPDATE_EDITOR_STATE') {
      return {
        ...state,
        editorState,
      };
    }
    return state;
} */